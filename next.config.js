/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'storage.googleapis.com',
            'm.media-amazon.com',
            'flxt.tmsimg.com'
        ]
    }
}

module.exports = nextConfig
